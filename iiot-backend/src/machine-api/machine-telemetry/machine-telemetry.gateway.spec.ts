import { MachineTelemetryGateway } from './machine-telemetry.gateway';

describe('MachineTelemetryGateway', () => {
  it('should be defined', () => {
    expect(MachineTelemetryGateway).toBeDefined();
  });
});
