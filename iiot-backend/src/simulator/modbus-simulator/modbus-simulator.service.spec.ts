import { ModbusSimulatorService } from './modbus-simulator.service';

describe('ModbusSimulatorService', () => {
  it('should be defined', () => {
    expect(ModbusSimulatorService).toBeDefined();
  });
});
