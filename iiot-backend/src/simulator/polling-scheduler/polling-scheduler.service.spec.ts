import { PollingSchedulerService } from './polling-scheduler.service';

describe('PollingSchedulerService', () => {
  it('should be defined', () => {
    expect(PollingSchedulerService).toBeDefined();
  });
});
